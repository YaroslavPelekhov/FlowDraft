# Identity-aware CFM with semantic retrieval from the full frozen vocabulary.
max_steps: 300
batch_size: 1
num_anchor_blocks: 32
candidate_count: 128
base_candidate_count: 96
retrieval_count: 32
token_code_dim: 32
token_code_seed: 20260723
hidden_size: 512
num_layers: 3
num_heads: 8
dropout: 0.0
learning_rate: 3.0e-4
warmup_ratio: 0.08
weight_decay: 0.01
max_grad_norm: 1.0
root_ce_weight: 1.0
diagonal_ce_weight: 0.5
ecld_weight: 0.25
temporal_weight: 0.02
retrieval_weight: 0.5
prefix_weight_decay: 0.9
dtype: bf16
attn_implementation: eager
eval_every: 50
eval_batches: 8
eval_anchor_blocks: 32
early_stopping_patience: 3
seed: 20260723
num_workers: 2
log_every: 10
