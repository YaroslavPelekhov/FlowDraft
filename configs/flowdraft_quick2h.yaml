upstream_dir: upstream_orthrus
base_model: Qwen/Qwen3-1.7B
train_manifest: /tmp/flowdraft_storage/nemotron_quick_packed/manifest.json
output_dir: /dev/shm/flowdraft_runs/flowdraft_quick2h
block_size: 32
mask_token_id: 151669
num_anchor_blocks: 32
epochs: 1
max_steps: 600
batch_size: 1
gradient_accumulation_steps: 32
learning_rate: 2.0e-4
warmup_ratio: 0.05
weight_decay: 0.0
max_grad_norm: 1.0
dtype: bf16
attn_implementation: sdpa
temperature: 1.0
kl_reduction: tokenmean
flow_state_min: 0.0
flow_state_max: 0.75
hard_ce_weight: 0.5
prefix_loss_weight: 1.0
prefix_kl_weight: 0.5
prefix_weight_decay: 0.9
consistency_weight: 0.05
consistency_start_step: 100
seed: 17
num_workers: 2
log_every: 10
eval_every: 100
eval_batches: 8
eval_anchor_blocks: 8
save_every: 100
save_trainer_state: false
gradient_checkpointing: false
compile: false
