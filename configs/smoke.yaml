upstream_dir: upstream_orthrus
base_model: Qwen/Qwen3-1.7B
train_manifest: data/smoke_packed/manifest.json
output_dir: outputs/smoke
block_size: 32
mask_token_id: 151669
num_anchor_blocks: 4
epochs: 1
max_steps: 5
batch_size: 1
gradient_accumulation_steps: 1
learning_rate: 2.0e-4
warmup_ratio: 0.0
max_grad_norm: 1.0
dtype: bf16
attn_implementation: sdpa
num_workers: 0
log_every: 1
save_every: 5
gradient_checkpointing: false
compile: false
